define(["Magento_Ui/js/grid/columns/column"],function(Column){"use strict";const language='en-US';const fraction=2;const options={minimumFractionDigits:fraction,maximumFractionDigits:fraction};const formatter=new Intl.NumberFormat(language,options);const fnFormat=function(num){const value=Number(num);const result=formatter.format(value);return result;}
return Column.extend({defaults:{bodyTmpl:'Flancer32_Base/grid/cells/number',},getLabel:function(row){const num=Column.prototype.getLabel.apply(this,[row]);return fnFormat(num);}});});